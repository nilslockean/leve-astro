<ul class="icon-list">
  <slot />
</ul>

<style>
  .icon-list {
    display: flex;
    flex-direction: column;
    flex-wrap: wrap;
    gap: calc(var(--spacing) * 2);
    margin-left: 0;
  }
</style>
